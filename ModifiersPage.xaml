<Page x:Class="WindowsSnake.ModifiersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      xmlns:local="clr-namespace:WindowsSnake"
      mc:Ignorable="d" 
      d:DesignHeight="732" d:DesignWidth="700"
      Title="ModifiersPage">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Text="Modifiers" FontSize="50px" FontFamily="/Assets/SnakeDemo-lgEeD.otf#Snake Demo" HorizontalAlignment="Center"/>
      <TextBlock x:Name="ScoreMultiplierText" FontSize="35px" FontFamily="Arial" HorizontalAlignment="Center" FontWeight="Bold"/>
    </StackPanel>

    <ListView x:Name="ModifiersListView"
              Grid.Row="1"
              Margin="10,10,10,10"
              ScrollViewer.VerticalScrollBarVisibility="Auto">
      <ListView.GroupStyle>
        <GroupStyle>
          <GroupStyle.HeaderTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Name}" 
                         FontWeight="Bold" 
                         FontSize="20" 
                         Margin="5"/>
            </DataTemplate>
          </GroupStyle.HeaderTemplate>
        </GroupStyle>
      </ListView.GroupStyle>
      <ListView.ItemTemplate>
        <DataTemplate>
          <StackPanel Orientation="Horizontal" Margin="5" CanVerticallyScroll="True">
            <CheckBox IsChecked="{Binding IsEnabled}"/>
            <TextBlock Text="{Binding Name}" Margin="5,0,0,0"/>
          </StackPanel>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

    <Button Grid.Row="2" Height="38" Width="100" VerticalAlignment="Bottom" HorizontalAlignment="Center" Content="Main Menu" Click="MainMenu_Click" Margin="0,0,0,10" Style="{StaticResource WindowButtonTriggers}"/>
    <Button Grid.Row="2" Height="38" Width="100" VerticalAlignment="Bottom" HorizontalAlignment="Left" Content="Mod Info" Click="Info_Click" Margin="10,0,0,10" Style="{StaticResource WindowButtonTriggers}"/>
    <Button Grid.Row="2" Height="38" Width="100" VerticalAlignment="Bottom" HorizontalAlignment="Right" Content="Save" Click="Save_Click" Margin="0,0,10,10" Style="{StaticResource WindowButtonTriggers}"/>
  </Grid>
</Page>